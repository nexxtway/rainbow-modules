import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import UniversalFormOverlay from '../../src/components/UniversalFormOverlay';
import * as stories from '../stories/UniversalFormOverlay.story.js';

<Meta title="Modules/Forms/Components/UniversalFormOverlay" component={UniversalFormOverlay} />

# Overview

`UniversalFormOverlay` is an abstraction on top of [react-final-form](https://final-form.org/react).
It shows an UniversalForm in a popover above the trigger element.

### This is a basic example.

<Preview>
    <Story name="UniversalFormOverlay">{stories.BasicUniversalFormOverlay()}</Story>
</Preview>

# Component props

<Props of={UniversalFormOverlay} />
