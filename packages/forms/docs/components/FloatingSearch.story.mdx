import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import FloatingSearch from '../../src/components/FloatingSearch';
import * as stories from '../stories/FloatingSearch.story.js';

<Meta title="Modules|Forms/Components" component={FloatingSearch} />

# Overview

`FloatingSearch` is a search that appears floating on top of all the elements of the page.

### This is an example with a button that display a FloatingSearch when click on it.

<Preview>
    <Story name="FloatingSearch">{stories.BasicFloatingSearch()}</Story>
</Preview>

# Component props

<Props of={FloatingSearch} />
