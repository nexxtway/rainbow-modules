import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import UniversalPickerOption from '../../src/components/UniversalPickerOption';
import * as stories from '../stories/UniversalPicker.story.js';

<Meta title="Modules|Forms/Components" component={UniversalPickerOption} />

# Overview

`UniversalPickerOption` is a component to represent a option of the UniversalPicker.

### This is a basic example.

<Preview>
    <Story name="UniversalPickerOption">{stories.BasicUniversalPicker()}</Story>
</Preview>

# Component props

<Props of={UniversalPickerOption} />
