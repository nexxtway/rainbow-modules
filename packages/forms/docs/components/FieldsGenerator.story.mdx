import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import FieldsGenerator from '../../src/components/FieldsGenerator';
import * as stories from '../stories/FieldsGenerator.story.js';

<Meta title="Modules/Forms/Components/FieldsGenerator" component={FieldsGenerator} />

# Overview

`FieldsGenerator` is a generator of [react-final-form](https://final-form.org/react) fields. You use the `schema` prop to define the form fields.

### This is a basic example.

<Preview>
    <Story name="FieldsGenerator">{stories.Basic()}</Story>
</Preview>

# Component props

<Props of={FieldsGenerator} />
