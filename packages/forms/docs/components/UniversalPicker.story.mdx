import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import UniversalPicker from '../../src/components/UniversalPicker';
import * as stories from '../stories/UniversalPicker.story.js';

<Meta title="Modules/Forms/Components/UniversalPicker" component={UniversalPicker} />

# Overview

`UniversalPicker` can be either radio buttons or checkboxes that are visually enhanced.

### This is a basic example.

<Preview>
    <Story name="UniversalPicker">{stories.BasicUniversalPicker()}</Story>
</Preview>

# Component props

<Props of={UniversalPicker} />
