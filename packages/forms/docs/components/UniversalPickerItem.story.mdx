import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import UniversalPickerItem from '../../src/components/UniversalPickerItem';
import * as stories from '../stories/UniversalPicker.story.js';

<Meta title="Modules|Forms/Components" component={UniversalPickerItem} />

# Overview

`UniversalPickerItem` is a component to manage and select images stored in Firebase Storage.

### This is a basic example.

<Preview>
    <Story name="UniversalPicker">{stories.BasicFormUniversalPicker()}</Story>
</Preview>

# Component props

<Props of={UniversalPickerItem} />
