import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import RecordPrimaryDetails from '../../src/components/RecordPrimaryDetails';
import * as stories from '../stories/RecordPrimaryDetails.story.js';

<Meta title="Modules|Record/Components" component={RecordPrimaryDetails} />

# Overview

`RecordPrimaryDetails` component is used to display the details of a record in a data collection. The `RecordPrimaryDetails` also allows you to display the different types of fields in different ways.

### This is an example with the RecordPrimaryDetails.

<Preview>
    <Story name="RecordPrimaryDetails">{stories.basicRecordPrimaryDetails()}</Story>
</Preview>

# Component props

<Props of={RecordPrimaryDetails} />
