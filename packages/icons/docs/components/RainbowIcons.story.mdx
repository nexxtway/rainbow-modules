import { Meta } from '@storybook/addon-docs/blocks';
import RainbowIconsSearch from '../stories/rainbowIconsSearch';

<Meta title="Icons" />

# Icons

Rainbow icons are delightful, beautifully crafted symbols for common actions and items.

## Installation

Install the icons using npm.

```
$npm install @rainbow-modules/icons

```

Install the icons using yarn.

```
$ yarn add @rainbow-modules/icons

```

## Using the icons in your web app

It's easy to incorporate icons into your web page. Here's a samll example:

```
<Clock />

```

<RainbowIconsSearch />
