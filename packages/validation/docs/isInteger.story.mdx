<Meta title="Modules|Validation/isInteger" />

# Overview

`isInteger` checks if the given value is an integer number.
If this returns true `isNumber` will return true as well.

```js
isInteger(3);
// returns true

isInteger(3.14);
// returns false

isInteger('any string value');
// returns false
```
