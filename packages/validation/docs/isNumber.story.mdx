<Meta title="Validation/isNumber" />

# Overview

`isNumber` checks if the given value is a number.
Unlike most isNumber checks this function does not consider
NaN to be a number.

```js
isNumber(300);
// returns true

isNumber('foobar');
// returns false
```
