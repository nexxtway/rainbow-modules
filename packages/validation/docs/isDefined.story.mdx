<Meta title="Modules|Validation/isDefined" />

# Overview

`isDefined` checks if the given value is not null or undefined.

```js
isDefined('foobar');
// returns true

isDefined(undefined);
// returns false

isDefined(null);
// returns false
```
