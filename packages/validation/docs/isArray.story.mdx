<Meta title="Validation/isArray" />

# Overview

`isArray` checks if the given value is an array.

```js
isArray([]);
// returns true

isArray([1, '2']);
// returns true

isArray({});
// returns false
```
