import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import * as stories from '../stories/useCurrentUser.story';

<Meta title="Modules/firebase-hooks/Hooks/useCurrentUser" />

# Overview

`useCurrentUser` hook will give you the same as `firebase.auth().currentUser` return. The benifit is that you don't have to care about importing the firebase
instance.

<Preview>
    <Story name="useCurrentUser">{stories.gettingCurrentUser()}</Story>
</Preview>
