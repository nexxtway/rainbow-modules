import { defineMessages } from 'react-intl';

const messages = defineMessages({
    copiedToClipboard: {
        id: 'CodeBlock.copiedToClipboard',
        defaultMessage: 'Copied to Clipboard',
    },
});

export default messages;
