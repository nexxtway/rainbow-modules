import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Mapbox from '../../src/components/Mapbox';
import * as stories from '../stories/Mapbox.story.js';

<Meta title="Modules|Dashboard/Components" component={Mapbox} />

# Overview

`Mapbox` is a component for Mapbox solution.

### This is a basic example.

<Preview>
    <Story name="Mapbox">{stories.simpleMap()}</Story>
</Preview>

# Component props

<Props of={Mapbox} />
