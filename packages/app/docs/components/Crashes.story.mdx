import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Crashes from '../../src/components/Crashes';
import * as stories from '../stories/appCrashes.story.js';

<Meta title="Modules|App/Components" component={Crashes} />

# Overview

`Crashes` component is used to catch an error in your app and display it in a user friendly way. This functionality is included in `RainbowFirebaseApp`.

### This is an example with the Crashes component.

<Preview>
    <Story name="Crashes">{stories.DefaultCrashes()}</Story>
</Preview>

# Component props

<Props of={Crashes} />
