import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import ColumnHeaderFilterText from '../../src/components/ColumnHeaderFilterText';
import * as stories from '../stories/ColumnHeaderFilterText.story.js';

<Meta
    title="Modules/Listview/Components/ColumnHeaderFilterText"
    component={ColumnHeaderFilterText}
/>

# Overview

`ColumnHeaderFilterText` is a component that allows you to add the functionality of filtering by text to the table column headers.

### This is basic example that allows you to filter.

<Preview>
    <Story name="ColumnHeaderFilterText">{stories.BasicColumnHeaderFilterText()}</Story>
</Preview>

# Component props

<Props of={ColumnHeaderFilterText} />
