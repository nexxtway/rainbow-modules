import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import FloatingSearchButtonIcon from '../../src/components/FloatingSearchButtonIcon';
import * as stories from '../stories/FloatingSearchButtonIcon.story.js';

<Meta
    title="Modules/Listview/Components/FloatingSearchButtonIcon"
    component={FloatingSearchButtonIcon}
/>

# Overview

`FloatingSearchButtonIcon` is a button that displays the `FloatingSearch` module when clicking on it.

### This is an example with a FloatingSearchButtonIcon .

<Preview>
    <Story name="FloatingSearchButtonIcon">{stories.BasicFloatingSearchButtonIcon()}</Story>
</Preview>

# Component props

<Props of={FloatingSearchButtonIcon} />
