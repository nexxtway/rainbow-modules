import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import BatchActionsBar from '../../src/components/BatchActionsBar';
import * as stories from '../stories/BatchActionsBar.story.js';

<Meta title="Modules|Listview/Components" component={BatchActionsBar} />

# Overview

`BatchActionsBar` is a component that formats a cell in a table column depending on status. It is configured with the success, warning, and error status, and with a text-transform capitalize by default, but it can be customized with other colors, status, and text-transform properties.

### This is a basic example with the colors and status by default.

# Component props

<Props of={BatchActionsBar} />
