import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Scales from '../../src/components/Scales';
import * as stories from '../stories/Chart.story.js';

<Meta title="Modules|Charts/Components" component={Scales} />

# Overview

`Scales` component acts as a wrapper for the chart axis.

### This is a basic example.

<Preview>
    <Story name="Scales">{stories.StackedBarChart()}</Story>
</Preview>

# Component props

<Props of={Scales} />
